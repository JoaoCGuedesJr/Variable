// 1.  declare uma variável de nome weight
var weight;

// 2.  Que tipo de dado é a variavém acima?
cosole.log(typeof weigth);

/* 
    3. Declare uma variável e atribua valores para cada um dos dados:
        * nome:  String
        * age: number (integer)
        * stars number (float)
        *  isSubscribed: boolean
*/

let name = "João";
let age = 28;
let stars = 5.2;
let isSubscribed = true;

/* 
    4. a variável studant abaixo é de que tipo de dado? 

    4.1 Atribua a ela as mesmas propriedades e valores do exercício 3.

    4.2 mostre no console a seguinte mensagem

        <name> de idade <age> pesa <weight> kg

        Atenção, substitua <name> <age> e <weight> pelos valores de cada propriedade do objeto

*/
let studant = {
  name: "João",
  age: 28,
  weight: 90.2,
  isSubscribed: true,
};

console.log(typeof studant);

console.log(
  `${student.name} de idade ${student.age} pesa ${student.weight} kg`
);

/*
    5. Declare uma variável do tipo Arrey de nome students e atribua a ela nenhum valor, ou sejja, somente o array vazio
*/
